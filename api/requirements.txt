# Core API
fastapi
uvicorn
python-multipart
pydantic
starlette
requests

# Core ML + Vision dependencies
--extra-index-url https://download.pytorch.org/whl/cpu

# Strongly pin numpy<2 first, before anything else resolves
numpy==1.26.4

# PyTorch CPU builds
torch==2.2.2
torchvision==0.17.2

# Vision / video
opencv-python-headless>=4.10.0.84

# GCP
google-cloud-storage